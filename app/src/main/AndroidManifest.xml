<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.chelseaapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launher_square"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MenuTrophy"></activity>
        <activity android:name=".MatchFixturesActivity" />
        <activity android:name=".ClubHistoryActivity" />
        <activity android:name=".MenuPartners" />
        <activity android:name=".About" />
        <activity android:name=".PlayerDetail" />
        <activity android:name=".MenuPlayer" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>